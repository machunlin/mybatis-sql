<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.vipmro.hades.oms.mapper.FreightStrategyLogisticsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="net.vipmro.hades.oms.model.FreightStrategyLogistics">
        <id column="id" property="id" />
        <result column="freight_strategy_id" property="freightStrategyId" />
        <result column="province" property="province" />
        <result column="province_code" property="provinceCode" />
        <result column="city" property="city" />
        <result column="city_code" property="cityCode" />
        <result column="logistics_time" property="logisticsTime" />
        <result column="minimum_price" property="minimumPrice" />
        <result column="weight_univalent" property="weightUnivalent" />
        <result column="volume_univalent" property="volumeUnivalent" />
        <result column="delivery_fee" property="deliveryFee" />
        <result column="service_fee" property="serviceFee" />
        <result column="remark" property="remark" />
        <result column="create_by" property="createBy" />
        <result column="create_by_name" property="createByName" />
        <result column="create_at" property="createAt" />
        <result column="update_by" property="updateBy" />
        <result column="update_by_name" property="updateByName" />
        <result column="update_at" property="updateAt" />
        <result column="status" property="status" />
        <result column="version" property="version" />
        <result column="trace_no" property="traceNo" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, freight_strategy_id AS freightStrategyId, province, province_code AS provinceCode, city, city_code AS cityCode, logistics_time AS logisticsTime, minimum_price AS minimumPrice, weight_univalent AS weightUnivalent, volume_univalent AS volumeUnivalent, delivery_fee AS deliveryFee, service_fee AS serviceFee, remark, create_by AS createBy, create_by_name AS createByName, create_at AS createAt, update_by AS updateBy, update_by_name AS updateByName, update_at AS updateAt, status, version, trace_no AS traceNo
    </sql>
    <!-- 通用别名结果列 -->
    <sql id="Alias_Column_List">
        a.id,
        a.freight_strategy_id AS freightStrategyId,
        a.province,
        a.province_code AS provinceCode,
        a.city,
        a.city_code AS cityCode,
        a.logistics_time AS logisticsTime,
        a.minimum_price AS minimumPrice,
        a.weight_univalent AS weightUnivalent,
        a.volume_univalent AS volumeUnivalent,
        a.delivery_fee AS deliveryFee,
        a.service_fee AS serviceFee,
        a.remark,
        a.create_by AS createBy,
        a.create_by_name AS createByName,
        a.create_at AS createAt,
        a.update_by AS updateBy,
        a.update_by_name AS updateByName,
        a.update_at AS updateAt,
        a.status,
        a.version,
        a.trace_no AS traceNo,
        </sql>

</mapper>
