<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.vipmro.athena.directory.service.mapper.StaffMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="net.vipmro.athena.directory.service.model.Staff">
        <id column="id" property="id" />
        <result column="passport_id" property="passportId" />
        <result column="staff_no" property="staffNo" />
        <result column="real_name" property="realName" />
        <result column="sex" property="sex" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="head_path" property="headPath" />
        <result column="org_id" property="orgId" />
        <result column="org_name" property="orgName" />
        <result column="post_id" property="postId" />
        <result column="post_name" property="postName" />
        <result column="post_grade" property="postGrade" />
        <result column="join_at" property="joinAt" />
        <result column="leave_at" property="leaveAt" />
        <result column="status" property="status" />
        <result column="version" property="version" />
        <result column="create_at" property="createAt" />
        <result column="create_by" property="createBy" />
        <result column="create_by_name" property="createByName" />
        <result column="update_at" property="updateAt" />
        <result column="update_by" property="updateBy" />
        <result column="update_by_name" property="updateByName" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, passport_id AS passportId, staff_no AS staffNo, real_name AS realName, sex, phone, email, head_path AS headPath, org_id AS orgId, org_name AS orgName, post_id AS postId, post_name AS postName, post_grade AS postGrade, join_at AS joinAt, leave_at AS leaveAt, status, version, create_at AS createAt, create_by AS createBy, create_by_name AS createByName, update_at AS updateAt, update_by AS updateBy, update_by_name AS updateByName
    </sql>
    <!-- 通用别名结果列 -->
    <sql id="Alias_Column_List">
        a.id,
        a.passport_id AS passportId,
        a.staff_no AS staffNo,
        a.real_name AS realName,
        a.sex,
        a.phone,
        a.email,
        a.head_path AS headPath,
        a.org_id AS orgId,
        a.org_name AS orgName,
        a.post_id AS postId,
        a.post_name AS postName,
        a.post_grade AS postGrade,
        a.join_at AS joinAt,
        a.leave_at AS leaveAt,
        a.status,
        a.version,
        a.create_at AS createAt,
        a.create_by AS createBy,
        a.create_by_name AS createByName,
        a.update_at AS updateAt,
        a.update_by AS updateBy,
        a.update_by_name AS updateByName,
        </sql>

</mapper>
