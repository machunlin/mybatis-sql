<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.vipmro.hades.bdm.mapper.CategoryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="net.vipmro.hades.bdm.model.Category">
        <id column="id" property="id" />
        <result column="code" property="code" />
        <result column="name" property="name" />
        <result column="short_name" property="shortName" />
        <result column="pid" property="pid" />
        <result column="tax_no" property="taxNo" />
        <result column="level" property="level" />
        <result column="is_leaf" property="isLeaf" />
        <result column="icon" property="icon" />
        <result column="brief" property="brief" />
        <result column="sort_no" property="sortNo" />
        <result column="has_attr" property="hasAttr" />
        <result column="has_goods" property="hasGoods" />
        <result column="create_by" property="createBy" />
        <result column="create_by_name" property="createByName" />
        <result column="create_at" property="createAt" />
        <result column="update_by" property="updateBy" />
        <result column="update_by_name" property="updateByName" />
        <result column="update_at" property="updateAt" />
        <result column="status" property="status" />
        <result column="version" property="version" />
        <result column="trace_no" property="traceNo" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, code, name, short_name AS shortName, pid, tax_no AS taxNo, level, is_leaf AS isLeaf, icon, brief, sort_no AS sortNo, has_attr AS hasAttr, has_goods AS hasGoods, create_by AS createBy, create_by_name AS createByName, create_at AS createAt, update_by AS updateBy, update_by_name AS updateByName, update_at AS updateAt, status, version, trace_no AS traceNo
    </sql>
    <!-- 通用别名结果列 -->
    <sql id="Alias_Column_List">
        a.id,
        a.code,
        a.name,
        a.short_name AS shortName,
        a.pid,
        a.tax_no AS taxNo,
        a.level,
        a.is_leaf AS isLeaf,
        a.icon,
        a.brief,
        a.sort_no AS sortNo,
        a.has_attr AS hasAttr,
        a.has_goods AS hasGoods,
        a.create_by AS createBy,
        a.create_by_name AS createByName,
        a.create_at AS createAt,
        a.update_by AS updateBy,
        a.update_by_name AS updateByName,
        a.update_at AS updateAt,
        a.status,
        a.version,
        a.trace_no AS traceNo,
        </sql>

</mapper>
