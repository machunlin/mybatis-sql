<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.vipmro.hades.oms.mapper.PromotionGiftMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="net.vipmro.hades.oms.model.PromotionGift">
        <id column="id" property="id" />
        <result column="promotion_id" property="promotionId" />
        <result column="effect_rank" property="effectRank" />
        <result column="effect_value" property="effectValue" />
        <result column="type" property="type" />
        <result column="gift_id" property="giftId" />
        <result column="code" property="code" />
        <result column="name" property="name" />
        <result column="total_num" property="totalNum" />
        <result column="single_num" property="singleNum" />
        <result column="given_num" property="givenNum" />
        <result column="price" property="price" />
        <result column="amount" property="amount" />
        <result column="create_by" property="createBy" />
        <result column="create_by_name" property="createByName" />
        <result column="create_at" property="createAt" />
        <result column="update_by" property="updateBy" />
        <result column="update_by_name" property="updateByName" />
        <result column="update_at" property="updateAt" />
        <result column="status" property="status" />
        <result column="version" property="version" />
        <result column="trace_no" property="traceNo" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, promotion_id AS promotionId, effect_rank AS effectRank, effect_value AS effectValue, type, gift_id AS giftId, code, name, total_num AS totalNum, single_num AS singleNum, given_num AS givenNum, price, amount, create_by AS createBy, create_by_name AS createByName, create_at AS createAt, update_by AS updateBy, update_by_name AS updateByName, update_at AS updateAt, status, version, trace_no AS traceNo
    </sql>
    <!-- 通用别名结果列 -->
    <sql id="Alias_Column_List">
        a.id,
        a.promotion_id AS promotionId,
        a.effect_rank AS effectRank,
        a.effect_value AS effectValue,
        a.type,
        a.gift_id AS giftId,
        a.code,
        a.name,
        a.total_num AS totalNum,
        a.single_num AS singleNum,
        a.given_num AS givenNum,
        a.price,
        a.amount,
        a.create_by AS createBy,
        a.create_by_name AS createByName,
        a.create_at AS createAt,
        a.update_by AS updateBy,
        a.update_by_name AS updateByName,
        a.update_at AS updateAt,
        a.status,
        a.version,
        a.trace_no AS traceNo,
        </sql>

</mapper>
